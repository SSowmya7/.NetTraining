using System;
using System.Collections;

namespace CollectionPractice
{
    class Program
    {
        //static void Main(string[] args)
        //{
        //    // Queue Example
        //    Queue queue = new Queue();
        //    Console.WriteLine("Enter names to add to the queue (enter 'done' to stop):");
        //    string input;
        //    while ((input = Console.ReadLine()) != "done")
        //    {
        //        queue.Enqueue(input);
        //    }

        //    Console.WriteLine("\nQueue:");
        //    while (queue.Count > 0)
        //    {
        //        Console.WriteLine(queue.Dequeue());
        //    }

        //    // Stack Example
        //    Stack stack = new Stack();
        //    Console.WriteLine("\nEnter items to add to the stack (enter 'done' to stop):");
        //    while ((input = Console.ReadLine()) != "done")
        //    {
        //        stack.Push(input);
        //    }

        //    Console.WriteLine("\nStack:");
        //    while (stack.Count > 0)
        //    {
        //        Console.WriteLine(stack.Pop());
        //    }

        //    // Hashtable Example
        //    Hashtable hashtable = new Hashtable();
        //    Console.WriteLine("\nEnter key-value pairs to add to the hashtable (enter 'done' to stop):");
        //    while ((input = Console.ReadLine()) != "done")
        //    {
        //        string[] keyValue = input.Split(' ');
        //        if (keyValue.Length == 2)
        //        {
        //            hashtable.Add(keyValue[0], keyValue[1]);
        //        }
        //    }

        //    Console.WriteLine("\nHashtable:");
        //    foreach (DictionaryEntry entry in hashtable)
        //    {
        //        Console.WriteLine($"Key: {entry.Key}, Value: {entry.Value}");
        //    }
        //}
    }
}

